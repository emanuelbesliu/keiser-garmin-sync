apiVersion: v1
kind: Service
metadata:
  name: keiser-garmin-sync
  labels:
    app: keiser-garmin-sync
spec:
  type: ClusterIP
  selector:
    app: keiser-garmin-sync
  ports:
    - name: http
      port: 80
      targetPort: 8096
